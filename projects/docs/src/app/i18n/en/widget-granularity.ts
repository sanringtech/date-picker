import type { WidgetRangeDictionary } from '../i18n.types';

export const widgetGranularity: WidgetRangeDictionary = {
  pageTitle: 'Widget Layer — Granularity',
  pageDescription:
    'Composed widget layer (<code class="font-mono text-xs">@sanring/date-picker-widget</code>): DateGranularityPickerComponent — month / quarter / year single-select picker.',
  cardTitle: 'DateGranularityPicker (W6-single, month mode)',
  cardDescription:
    'Displays a 12-cell month grid; arrow keys move focus, PageUp/PageDown jump years, Enter/Space selects, Escape closes. Switch the <code class="font-mono text-xs">granularity</code> input to "quarter" (4 cells) or "year" (sliding window) mode.',
  placeholder: 'Select month',
  selectedLabel: 'Selected: ',
  notSelected: 'Not selected',
};

import type { WidgetRangeDictionary } from '../i18n.types';

export const widgetGranularityMulti: WidgetRangeDictionary = {
  pageTitle: 'Widget Layer — Granularity Multi',
  pageDescription:
    'Composed widget layer (<code class="font-mono text-xs">@sanring/date-picker-widget</code>): DateGranularityMultiPickerComponent — year toggle multi-select picker.',
  cardTitle: 'DateGranularityMultiPicker (year multi mode)',
  cardDescription:
    'Each click toggles a year; selected years appear as chips and can be removed individually or cleared at once. The overlay stays open until Escape or an outside click. Switch the <code class="font-mono text-xs">granularity</code> input for month or quarter multi-select.',
  placeholder: 'Select years',
  selectedLabel: 'Selected: ',
  notSelected: 'Not selected',
};
